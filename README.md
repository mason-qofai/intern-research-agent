# intern-research-agent

Scaffold for an internal research agent. Fill in what it actually does once module 5 defines the workflow.

## Structure

intern-research-agent/
├── prompts/    # Prompt templates used by the pipeline
├── skills/     # Claude Code skills invoked during runs
├── scripts/    # Executable entry points and utilities
├── sources/    # Raw input material
├── dossiers/   # Generated output documents
├── audits/     # Logs and review records of past runs
├── evals/      # Test cases and scoring for prompt/skill quality
└── README.md

## Setup

1. Clone the repo.
2. Install dependencies (fill in once known).
3. Set any required environment variables (fill in once known).

## Run

Fill in the actual run command once the pipeline exists.

## Notes

Add anything a new contributor needs before touching prompts/ or skills/.
